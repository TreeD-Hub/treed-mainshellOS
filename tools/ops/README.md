# Operator Tools

This directory is reserved for manual operator scripts.

Rules:
- Scripts here are not called from `loader/loader.sh`.
- Runtime camera scripts live in `runtime-scripts/treed-cam/`.
- Legacy diagnostics scripts (`health-check.sh`, `diag-collect.sh`) are removed.
